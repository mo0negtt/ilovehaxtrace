export default function Home() {
  // Página Home retirada — placeholder temporal para evitar roturas
  return (
    <div className="min-h-screen flex items-center justify-center bg-background">
      <div className="text-center p-8">
        <h1 className="text-2xl font-bold">Página eliminada</h1>
        <p className="text-muted-foreground mt-2">La página Home ha sido removida del proyecto.</p>
      </div>
    </div>
  );
}
